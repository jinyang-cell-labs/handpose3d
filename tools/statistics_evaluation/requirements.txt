# Python deps for analysing/visualising the handpose JSONL logs produced by
# ros2_ws/src/mediapie_landmarks_extraction. Installed into ./.venv by
# start_evaluation.bash. Pins are lower bounds — bump freely.
numpy>=1.26          # arrays / linear algebra (DLT triangulation, etc.)
scipy>=1.11          # non-linear optimisation (least_squares) + spatial transforms
matplotlib>=3.8      # 2D and 3D plotting (mpl_toolkits.mplot3d)
PyQt5>=5.15          # interactive matplotlib backend (QtAgg) so windows open;
                     # self-contained wheel — needed because the pyenv venv has
                     # no tkinter/Qt of its own
# headless build: solvePnP/Rodrigues etc. WITHOUT bundled Qt — avoids a clash
# with matplotlib's QtAgg (PyQt5) backend that otherwise crashes the window.
opencv-python-headless>=4.8
pandas>=2.1          # tabular wrangling of the per-frame records
pyyaml>=6.0          # read the calibration yaml when needed
