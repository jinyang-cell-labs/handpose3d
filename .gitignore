# Virtual environments
.venv/
venv/
env/
ENV/

# Python bytecode / caches
__pycache__/
*.py[cod]
*$py.class
*.so

# Packaging / build artifacts
build/
dist/
*.egg-info/
.eggs/

# Test / coverage caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Jupyter
.ipynb_checkpoints/

# Generated keypoint output (written by write_keypoints_to_disk).
# Note: the calibration files in camera_parameters/*.dat are tracked inputs,
# so we intentionally do NOT ignore all *.dat — only the generated kpts_*.dat.
kpts_*.dat

# Per-frame PNGs rendered by show_3d_hands.py
figs/

# Downloaded MediaPipe Tasks model bundle (fetched by run.sh)
models/

# OS cruft
.DS_Store
Thumbs.db

# Editor / IDE
.vscode/
.idea/
*.swp
